# Environment Variables for daThiccNOW Minter
# Copy this file to .env.local and fill in your values

# Solana RPC Endpoint (optional, defaults to devnet)
# For production, use a dedicated RPC like Helius, QuickNode, or Alchemy
VITE_RPC_ENDPOINT=https://api.devnet.solana.com

# Pinata API JWT for IPFS uploads (optional for demo mode)
# Get your key at: https://app.pinata.cloud/developers/api-keys
VITE_PINATA_JWT=
